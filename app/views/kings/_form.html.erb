<%= form_for(king) do |f| %>
  <% if king.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(king.errors.count, "error") %> prohibited this king from being saved:</h2>

      <ul>
      <% king.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
